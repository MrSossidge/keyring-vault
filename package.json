{
  "name": "keyring-vault",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted, end-to-end encrypted API key vault. One HTML file plus a blob store that cannot read it.",
  "scripts": {
    "test": "node test/run.js",
    "serve": "node sync/server.js"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "license": "MIT"
}
